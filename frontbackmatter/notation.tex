	\newpage
    \refstepcounter{dummy}
    \pdfbookmark[1]{Notation}{notation}
    \markboth{\spacedlowsmallcaps{Notation}}{\spacedlowsmallcaps{Notation}}
    \chapter*{Notation}

    \section*{Frequently used terms}%
    \vskip -2em
    \begin{tabularx}{\textwidth}{lX}
    	edit distance		& minimal cost to edit one sequence to another \\
    	alignment 			& correspondence between letters of two sequences \\
		\,$\sbullet$ global 			& fully aligning two sequences to each other \\
		\,$\sbullet$ semi-global		& fully aligning a query somewhere in a reference \\
		\,$\sbullet$ reads alignment 		& multiple semi-global alignments to the same reference \\[5pt]
    	dynamic programming & optimization technique by reusing partial results \\
    	\,$\sbullet$ explored state&  \\
    	\,$\sbullet$ expanded state&  \\
		\dijkstra's algorithm & uninfomred shortest path algorithm \\
    	\A algorithm 		& generalization of \dijkstra, informed by a heuristic \\
		\,$\sbullet$ admissible heuristic		& lower bound on the remaining shortest distance \\
    	graph reference		& a compressed representation of multiple references \\
    	trie index 			& tree structure with short reference subsequences \\
    	seed heuristic		& our proposed heuristic function for \A \\
		\,$\sbullet$ chaining & \\
		\,$\sbullet$ inexact matching & \\
		\,$\sbullet$ potential & maximal edits a seed can ``compensate'' for \\
    	performance 		& runtime and memory footprint \\
    	scaling	 			& change (not necessarily asymptotic) with bigger input \\
    \end{tabularx}

	\newpage
    \section*{Frequently used symbols}%
    \vskip -2em
    \begin{tabularx}{\textwidth}{lX}
		$\Sigma = \{A,C,G,T\}$ 			& alphabet \\
		$A, B \in \Sigma^*$				& sequences \\
		$q \in \Sigma^m$				& query/read \\
%    	edit operation		& insertion, deletion or substitution of a single letter \\
		$\cmatch, \csubst, \cins, \cdel \geq 0$ & edit costs for match, insertion, deletion or substitution of a single letter \\[5pt]
		$ed(A,B)$						& edit distance \\
		$\RG=(\RGV,\RGE)$               & reference graph \\[5pt]
%	Position in read & $q[i] \in \Sigma$, $i \in \{0,\dots,m-1\}$\\	
		$\st{u}{i}$ 					& state (subalignment of A)state heuristic function \\
		$\AG=(\AGV,\AGE)$ 				& alignment graph on states $\AGV$\\
		$g$								& best distance from start \\
		$h\colon V \to \Costs$ 			& heuristic function \\
		$f = g + h$						& estimated total distance \\
		$h\st{u}{i}$					& seed heuristic \\
		$s\in \seeds$					& seeds \\
		$k$ 							& seed length \\
		$m \in \matches_s$				& matches of seed $s$ \\
		$\bluecrumb{}, \yellowcrumb{}, \violetcrumb{}, \greencrumb{}$ & crumbs on nodes for match $m$ \\
		$D$								& trie depth \\
    \end{tabularx}
\endgroup
