\section{Prerequisites} \label{SEEDsec:prereq}

We define the task of alignment as a shortest path problem~(\cref{SEEDsec:task}) to
be solved using the \A~algorithm (\cref{SEEDsec:astar}).
%
% Because the \A~algorithm relies on a heuristic function, we also discuss which
% heuristic functions are \emph{admissible}, \ie, when the \A~algorithm is
% guaranteed to find a shortest path.

\input{\dir/prereq-task}
\input{\dir/prereq-astar}